{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "news-dissemination",
  "entities": [
    "dist/**/*.entity{.ts,.js}",
    "node_modules/nestjs-admin/**/*.entity.js"
  ],
  "migrations": ["migration/*.ts"],
  "synchronize": true,
  "cli": { "migrationsDir": "migration" }
}
