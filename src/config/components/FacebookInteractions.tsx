import { FilterPropertyProps } from 'adminjs';
import React, { useEffect } from 'react';

const FacebookInteractions = (props: FilterPropertyProps) => {
  useEffect(() => {});

  return <div>lol</div>;
};

export default FacebookInteractions;
